// Bootstrap / entry module. Punto de carga final (ver orden en idemodel.html).
// Hoy no hace nada: la inicialización vive en api.js (init/loadData) y ui.js.
// Se mantiene como módulo para no cambiar el orden de carga ni el <script> del HTML.
export {};
