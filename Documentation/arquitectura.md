Decisiones congeladas (arquitectura)

Esto es lo más importante a largo plazo.

👉 Ya definiste:

edges NO se guardan
edges se derivan

👉 y:

concept = relación dirigida (no grouping)

👉 y:

id = sistema
label = humano